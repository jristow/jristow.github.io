"use strict";

const semver = require(`semver`);

const _require = require(`ci-info`),
      isCI = _require.isCI;

if (semver.satisfies(process.version, `>=8`) && !isCI) {
  module.exports = require(`./ink`).default;
} else {
  module.exports = require(`./yurnalist`);
}